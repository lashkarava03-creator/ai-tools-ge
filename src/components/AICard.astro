---
const { name, description, category, link } = Astro.props;
---

<div class="ai-card">
  <h3>{name}</h3>
  <p>{description}</p>
  <span class="category">{category}</span>
 <a href={`/tools/${name.toLowerCase().replace(/\s+/g, '-')}`}>
  დეტალურად
</a>

</div>

<style>
.ai-card {
  border: 1px solid #e5e7eb;
  padding: 16px;
  border-radius: 12px;
  background: #fff;
}

.ai-card h3 {
  margin: 0 0 8px;
}

.ai-card p {
  font-size: 14px;
  color: #555;
}

.category {
  display: inline-block;
  margin-bottom: 8px;
  font-size: 12px;
  color: #2563eb;
}

.ai-card a {
  display: inline-block;
  margin-top: 10px;
  font-weight: bold;
}
</style>
